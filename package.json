{
    "name": "server",
    "version": "0.0.0",
    "private": true,
    "scripts": {
        "start": "npm install && cd server && npm install && node ./bin/www",
        "sync_database": "cd server && npm install && node ./scripts/sync_database.js"
    },
    "dependencies": {
        "dotenv": "^10.0.0"
    }
}
